apiVersion: v1
kind: ConfigMap
metadata:
  name: fastfood-config
  labels:
    app: fastfood-config
data:
  DATABASE_HOST: "postgres-service"
  DATABASE: "fast_food"
  DATABASE_PORT: "5432"
  DATABASE_USER: "fastfood"
  DATABASE_PASSWORD: "fastfood"
